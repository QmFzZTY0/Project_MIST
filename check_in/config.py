def config(inf):
    cookie = ''#目标cookie
    referer = ''#登录地址
    target = ''#访问目标
    if inf == 'cookie':
        return cookie
    elif inf == 'referer':
        return referer
    elif inf == 'target':
        return target
